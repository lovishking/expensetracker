# Static Files

This directory contains static files like CSS, JavaScript, and images for the Expense Tracker application.

Currently, the application uses Bootstrap CDN for styling, so this directory may be empty.

You can add custom CSS or JavaScript files here if needed.
